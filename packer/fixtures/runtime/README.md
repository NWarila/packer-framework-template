# Runtime Fixture Overlay Landing Zone

Runner repositories may overlay ephemeral runtime fixtures here when they call
`reusable-packer-framework-build.yaml`. Keep durable framework fixtures outside
this directory so the overlay boundary stays auditable.
