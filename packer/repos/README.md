# Runner Overlay Landing Zone

Runner repositories may overlay inventory data here when they call
`reusable-packer-framework-build.yaml`. This directory is intentionally not a
tracked content area for the framework itself; committed files should be limited
to this README and `.gitkeep`.
